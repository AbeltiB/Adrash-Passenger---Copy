import { router } from 'expo-router';
import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Colors, Spacing, BorderRadius } from '@/constants';
import { Card, PrimaryButton } from '@/features/passenger-booking/components/BookingUi';
import { useBookingFlowStore } from '@/features/passenger-booking/store/bookingFlowStore';
export default function ConfirmationScreen() { const f = useBookingFlowStore(); const b = f.pendingBooking; return <SafeAreaView style={styles.container} edges={['top', 'bottom']}><ScrollView contentContainerStyle={styles.content}><View style={styles.successCircle}><Text style={styles.checkmark}>✓</Text></View><Text style={styles.title}>Booking confirmed!</Text><Text style={styles.subtitle}>Show your ticket QR and booking reference at boarding.</Text><Card><Text style={styles.label}>REFERENCE</Text><Text style={styles.ref}>{b?.bookingReference ?? b?.id ?? 'Confirmed'}</Text><Text>Seats: {f.selectedSeats.join(', ')}</Text><Text>{f.origin} → {f.destination}</Text><Text>Pickup: {f.selectedPickup?.name}</Text></Card><PrimaryButton onPress={() => router.replace('/(tabs)/my-trips')}>View my trips</PrimaryButton></ScrollView></SafeAreaView>; }
const styles = StyleSheet.create({ container: { flex: 1, backgroundColor: Colors.background.secondary }, content: { padding: Spacing.lg, gap: Spacing.md, alignItems: 'stretch' }, successCircle: { alignSelf: 'center', width: 96, height: 96, borderRadius: 48, backgroundColor: Colors.semantic.successLight, alignItems: 'center', justifyContent: 'center' }, checkmark: { color: Colors.semantic.success, fontSize: 52, fontWeight: '900' }, title: { textAlign: 'center', fontSize: 26, fontWeight: '900', color: Colors.text.primary }, subtitle: { textAlign: 'center', color: Colors.text.tertiary }, label: { color: Colors.text.tertiary, fontWeight: '800' }, ref: { fontWeight: '900', color: Colors.brand.primary, fontSize: 22 }, qr: { borderRadius: BorderRadius.md } });
